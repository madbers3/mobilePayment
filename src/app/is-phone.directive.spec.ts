import { IsPhoneDirective } from './is-phone.directive';

describe('IsPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new IsPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
